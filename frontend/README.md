# SwipChat Frontend

Next.js App Router, TypeScript, Tailwind CSS, React context, hooks, and native WebSocket.

Setup:

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

The app expects the backend at `http://localhost:8000` and WebSocket server at `ws://localhost:8000`.
